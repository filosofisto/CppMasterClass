#ifndef __HYPERNEX_MATH__
#define __HYPERNEX_MATH__

#include <iostream>
#include <vector>

namespace hypernex
{
	void add_vector(std::vector<int>& a, const std::vector<int>& b);
	void sub_vector(std::vector<int>& a, const std::vector<int>& b);
}

#endif
